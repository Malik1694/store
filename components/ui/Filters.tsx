import React from 'react';

const Filters = () => {
  return (
    <div className="my-8">
      <h3 className="text-lg font-semibold mb-4">Filters</h3>
      {/* Price range, brand, etc. */}
    </div>
  );
};

export default Filters;
